#!/usr/bin/env bash
#Process 

PID=$(pgrep -f "4-to_infinity_and_beyond")

# Use gdb to attach and send SIGTERM
if [ -n "$PID" ]; then
    gdb -q --batch --pid="$PID" -ex "call (int)exit(0)" -ex "detach" -ex "quit" >/dev/null 2>&1
fi
